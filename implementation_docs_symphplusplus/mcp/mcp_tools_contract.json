{
  "worker_tools": [
    {
      "name": "claim_work_key",
      "purpose": "Claim a one-time work key/secret with required claimed_by owner identity and bind it to the current MCP session using the grant role. This is a temporary bootstrap/recovery tool, not a generic planning surface.",
      "required_arguments": ["secret", "claimed_by"],
      "reconnect_policy": "Reconnects are accepted only when the same claimed_by owner presents the same secret proof. Existing worker and architect grants can be bound; claim_work_key does not mint grants. Explicit state_key values retain initialized handshake state only and do not restore claimed sessions."
    },
    {
      "name": "get_current_assignment",
      "purpose": "Return the scoped assignment for the bound grant."
    },
    {
      "name": "read_context",
      "purpose": "Read context.md for the current work package."
    },
    {
      "name": "read_task_plan",
      "purpose": "Read task_plan.md for the current work package."
    },
    {
      "name": "update_task_plan",
      "purpose": "Patch the plan with optimistic concurrency."
    },
    {
      "name": "append_finding",
      "purpose": "Append a finding to the current work package."
    },
    {
      "name": "append_progress",
      "purpose": "Append a progress event to the current work package.",
      "readiness_policy": "For non-merge-gated policies, generic append_progress statuses tests_passed and review_<profile>_green satisfy focused-test and review-profile readiness; tool-owned metadata/blocker/status/scope events are ignored."
    },
    {
      "name": "set_status",
      "purpose": "Request a valid state transition."
    },
    {
      "name": "report_blocker",
      "purpose": "Record an active blocker."
    },
    {
      "name": "resolve_blocker",
      "purpose": "Record that an active blocker was cleared."
    },
    {
      "name": "add_comment",
      "purpose": "Add a redacted contextual note to the current worker's WorkPackage, a planned slice linked to it, or the parent WorkRequest of that linked slice.",
      "scope_policy": "Rejects sibling WorkPackages, unlinked planned slices, and unrelated WorkRequests without leaking target contents. Worker attribution is derived from the claimed MCP session. Comment bodies are capped at 4,000 characters."
    },
    {
      "name": "list_comments",
      "purpose": "List redacted contextual comments for a scoped WorkPackage, linked planned slice, or linked parent WorkRequest.",
      "scope_policy": "Uses the same worker target scope as add_comment. Results are capped at 100 comments per target."
    },
    {
      "name": "resolve_comment",
      "purpose": "Mark a scoped comment resolved with server-owned worker attribution and an optional resolution note.",
      "scope_policy": "A worker can resolve only comments attached to targets inside the current WorkPackage assignment scope; missing and foreign comment ids fail closed as out of scope. Resolution notes are capped at 1,000 characters."
    },
    {
      "name": "request_scope_expansion",
      "purpose": "Request broader scope; does not approve it.",
      "readiness_policy": "For investigation policies, this records the worker scope recommendation evidence."
    },
    {
      "name": "attach_branch",
      "purpose": "Attach branch metadata with the current branch head SHA.",
      "head_policy": "The latest branch head is the worker-declared current code head; PR metadata must match it for merge readiness."
    },
    {
      "name": "attach_pr",
      "purpose": "Attach PR metadata by URL or by number when the package or request supplies unambiguous owner/repo context."
    },
    {
      "name": "sync_pr",
      "purpose": "Refresh metadata for the already attached PR.",
      "required_arguments": ["metadata"],
      "sync_policy": "sync_pr must target the latest attached PR and must carry the synced head SHA in head_sha, metadata.head_sha, or metadata.head.sha."
    },
    {
      "name": "submit_review_package",
      "purpose": "Attach summary/tests/artifacts for the current head review package.",
      "head_policy": "head_sha is required on every submit_review_package call and must match the latest attached branch head.",
      "readiness_policy": "The latest review package for the current head is authoritative; older same-head packages are superseded."
    },
    {
      "name": "mark_ready",
      "purpose": "Move to ready state only if gates pass.",
      "ci_policy": "CI readiness is required only when the package policy required_gates includes ci_waiting. Without that gate, mark_ready may move directly from reviewing to the terminal ready status after the remaining readiness gates pass. The mcp_ci_required policy template explicitly requires the CI wait for MCP packages.",
      "post_ready_policy": "After mark_ready succeeds, new worker evidence writes are rejected as already_ready while idempotent replays remain available."
    }
  ],
  "bootstrap_tools": [
    {
      "name": "claim_private_handoff",
      "purpose": "Claim an existing worker or architect grant from redacted local-private-file handoff metadata and bind it to the current MCP session without exposing the raw secret.",
      "required_arguments": ["claimed_by"],
      "metadata_arguments": ["private_handoff", "mode", "path", "target", "grant_id", "display_key", "work_package_id", "database"],
      "scope_policy": "Supports only mode=local-private-file. The server reads the secret from the known Symphony++ private handoff store, requires the supplied path to match the managed metadata path, rejects arbitrary paths, traversal, missing files, directories, oversized files, and mismatched grant/display/work_package metadata, verifies the stored hash, then binds the session like claim_work_key.",
      "response_policy": "Returns redacted assignment/session metadata only. It never accepts or returns raw secrets, secret hashes, secret-bearing commands, private-store secret payloads, bearer tokens, API tokens, or MCP auth tokens."
    },
    {
      "name": "create_work_request",
      "purpose": "Create a local WorkRequest with creator provenance and return a redacted architect handoff/bootstrap payload.",
      "required_arguments": ["repo", "base_branch", "title", "request_kind", "description_or_human_description"],
      "optional_arguments": ["human_description", "workflow_mode", "constraints", "status", "claimed_by", "creator_kind", "created_by_kind", "creator_name", "created_by_name", "created_via"],
      "description_policy": "description is canonical, but human_description is accepted as the same required WorkRequest description field for current local/operator callers.",
      "provenance_policy": "creator_kind must be human, agent, operator, or system when supplied. If omitted for this MCP tool, creator_kind defaults to agent, created_via defaults to mcp, and creator_name defaults from caller-supplied claimed_by when available or mcp-agent otherwise.",
      "response_policy": "On success, returns the WorkRequest summary/detail including provenance, redacted architect handoff metadata, a non-secret claim owner for claim_private_handoff, and a launch prompt. If handoff mint/replay fails after WorkRequest creation, returns partial_success with the WorkRequest id and a non-duplicating manual architect-handoff replay hint while still withholding raw secret material."
    }
  ],
  "architect_tools": [
    {
      "name": "create_child_work_package",
      "purpose": "Create a phase_child work package inside the architect grant's frozen phase/repo/base-branch anchor scope; child repo, phase, parent, and base branch are constrained to that anchor.",
      "scope_policy": "Rejects mismatched phase_id, parent_id, repo, or base_branch, inherits the anchor base branch because there is no separate phase base-branch policy field, requires concrete nonempty child file globs, and revalidates anchor scope in the insert transaction. Empty anchor globs are allowed only when the child explicitly supplies nonempty, non-overbroad globs; nonempty anchor globs remain the upper bound."
    },
    {
      "name": "mint_child_worker_key",
      "purpose": "Mint a child-scoped worker grant for a same-phase phase_child package.",
      "scope_policy": "Worker grants are single-package grants, capabilities are limited to worker:claim and worker:lifecycle.transition, new mints are rejected while any active child-delegated worker grant exists for the same child, and unrelated normal worker grants are ignored. If the transaction-current architect grant has an explicit expiry, child expiry defaults to that timestamp and cannot exceed it. If the architect grant is non-expiring, child worker grants default to non-expiring and may use a future explicit expiry. This is the pre-production v1 contract and does not provide implicit replacement/remint behavior.",
      "response_policy": "The raw child worker secret is stored through SecretHandoff and is not returned in content or structuredContent. The response includes redacted non-secret worker_grant.secret_handoff bootstrap metadata, including claimed_by and run_mcp_command when generated by SecretHandoff, and worker_grant.secret_in_response=false. It omits secret and secret_returned_once, and bootstrap fields must not embed the raw worker secret.",
      "handoff_template_policy": "Optional template.secret_handoff may include only mode, store_dir, and claimed_by. Unexpected fields, blank values, and unsupported modes are rejected and cannot broaden grant capabilities.",
      "configuration_policy": "The MCP server must be configured with repo_root/--repo-root before child minting so handoff scripts are resolved from an operator-chosen repository root; minting fails before grant creation if the expected handoff script is missing there.",
      "handoff_failure_policy": "If private handoff storage or metadata persistence fails, the new mint must not leave a usable active child grant and errors must not expose raw worker secret material.",
      "revoke_cleanup_policy": "Child worker revoke does not delete persisted child handoffs in this v1 package; operators must treat private handoff cleanup as a separate follow-up."
    },
    {
      "name": "revoke_child_worker_key",
      "purpose": "Revoke one live child-delegated worker grant for a same-phase phase_child package, reset active/interrupted children to ready_for_worker, and make immediate remint available.",
      "required_capabilities": ["revoke:child_worker_key"],
      "required_arguments": ["grant_id", "reason"],
      "scope_policy": "Requires a live architect grant with frozen phase/repo/base-branch anchor scope. The target grant must be a live worker grant with child_worker_delegation provenance for a same-phase child inside that anchor. Children in claimed, planning, implementing, reviewing, ci_waiting, or blocked are reset to ready_for_worker after revoke. Unrelated grants, normal worker grants, sibling/out-of-scope children, already revoked or expired grants, and children in architect-controlled/closed/merged or terminal states are rejected.",
      "response_policy": "Returns redacted operator-useful metadata only: child work package projection after reset, revoked grant id/work_package_id/capabilities/expiry/revoked_at, recycle status with previous_child_status/new_child_status/status_reset/remint_available=true, and the audit event payload. It never returns raw worker secrets, work keys, display keys, secret hashes, bearer tokens, API tokens, private handoff payloads, or MCP auth tokens.",
      "audit_policy": "Appends a durable redacted child progress/audit event with type child_worker_key_revoke, source_tool revoke_child_worker_key, the child work_package_id, target grant_id, redacted recycle reason, revoked_at, previous_status, new_status, status_reset, and private_handoff_cleanup=not_attempted."
    },
    {
      "name": "list_work_requests",
      "purpose": "List WorkRequests scoped to an explicit phase architect grant's frozen repo/base branch.",
      "required_capabilities": ["read:work_request"],
      "arguments_policy": "Accepts only optional status. Repo and base_branch are never caller-supplied; they come from the live explicit phase architect grant's frozen scope.",
      "scope_policy": "Requires an explicit phase-scoped architect grant with frozen repo/base_branch scope and a matching claimed phase anchor. Legacy null phase_id architect grants are not supported for WorkRequest MCP reads and fail closed instead of deriving scope from a mutable anchor package. Missing or mismatched scope fails closed.",
      "response_policy": "Returns safe JSON WorkRequest cards with redacted text plus total_count, scope, and filters. Per-request count/status summaries are returned by read_work_request. The payload excludes work-key secrets, API tokens, private handoff payloads, and worker secret material."
    },
    {
      "name": "read_work_request",
      "purpose": "Read one scoped WorkRequest with clarification questions, decision log entries, visible planned slices, and count/status summaries.",
      "required_capabilities": ["read:work_request"],
      "arguments_policy": "Requires work_request_id, accepts optional include_planning_scratch, and accepts no repo/base-branch override.",
      "scope_policy": "Requires an explicit phase-scoped architect grant with frozen repo/base_branch scope and a matching claimed phase anchor. Legacy null phase_id architect grants are not supported for WorkRequest MCP reads and fail closed instead of deriving scope from a mutable anchor package. The target WorkRequest must match that frozen repo and base branch. Missing or out-of-scope WorkRequests fail closed as not_found without leaking sibling content.",
      "response_policy": "Returns safe JSON with redacted WorkRequest detail, clarification questions, decision log entries, visible planned slices, and summary counts. Skipped never-dispatched planned slices with no WorkPackage and no planned-slice delivery record are hidden by default as planning scratch; include_planning_scratch exposes them for audit. The payload excludes work-key secrets, API tokens, private handoff payloads, and worker secret material.",
      "cleanup_policy": "Planning scratch rows must not be removed with direct SQLite deletion. A future persisted archive path requires an explicit schema-backed design."
    },
    {
      "name": "read_work_request_delivery_board",
      "purpose": "Read the scoped WorkRequest delivery-board projection for visible planned-slice closeout without broad package visibility.",
      "required_capabilities": ["read:work_request"],
      "arguments_policy": "Requires work_request_id, accepts optional include_planning_scratch, and accepts no repo/base-branch override.",
      "scope_policy": "Requires an explicit phase-scoped architect grant with frozen repo/base_branch scope and a matching claimed phase anchor. The target WorkRequest must match that frozen repo and base branch. Missing or out-of-scope WorkRequests fail closed as not_found without leaking sibling content.",
      "response_policy": "Returns safe JSON with the scoped WorkRequest status projection and delivery_board. Skipped never-dispatched planned slices with no WorkPackage and no planned-slice delivery record are hidden by default as planning scratch; include_planning_scratch exposes them with planning_classification=planning_scratch. The payload excludes work-key secrets, API tokens, private handoff payloads, and worker secret material.",
      "cleanup_policy": "Planning scratch rows must not be removed with direct SQLite deletion. A future persisted archive path requires an explicit schema-backed design."
    },
    {
      "name": "set_work_request_status",
      "purpose": "Move a scoped WorkRequest between valid statuses with optimistic current_status checking.",
      "required_capabilities": ["write:work_request"],
      "required_arguments": ["work_request_id", "current_status", "next_status"],
      "scope_policy": "Requires an explicit phase-scoped architect grant with frozen repo/base_branch scope and a matching claimed phase anchor. The target WorkRequest must match that frozen repo and base branch before status mutation. Legacy null phase_id architect grants, missing snapshots, drifted anchors, worker sessions, anonymous sessions, stale sessions, revoked sessions, and architect sessions without write:work_request fail closed.",
      "response_policy": "Returns safe JSON with a minimal updated WorkRequest status projection plus scope and status metadata. It exposes the existing WorkRequest status update service as the explicit MCP status primitive and excludes read_work_request detail fields, work-key secrets, API tokens, private handoff payloads, and worker secret material."
    },
    {
      "name": "ask_work_request_question",
      "purpose": "Add a clarification question to a scoped WorkRequest.",
      "required_capabilities": ["write:work_request"],
      "required_arguments": ["work_request_id", "category", "question", "why_needed"],
      "optional_arguments": ["asked_by_agent_run_id", "decision_prompt"],
      "scope_policy": "Requires an explicit phase-scoped architect grant with frozen repo/base_branch scope and verifies the target WorkRequest is in scope before creating the question. This tool does not auto-transition the parent WorkRequest; architects use set_work_request_status for explicit status movement.",
      "response_policy": "Returns safe JSON with the redacted created clarification question, including the optional structured decision_prompt when present, a minimal parent WorkRequest status projection, scope, and status metadata. It excludes read_work_request detail fields, work-key secrets, API tokens, private handoff payloads, and worker secret material."
    },
    {
      "name": "answer_work_request_question",
      "purpose": "Answer an open clarification question that belongs to a scoped WorkRequest.",
      "required_capabilities": ["write:work_request"],
      "required_arguments": ["work_request_id", "question_id", "current_status", "answer"],
      "optional_arguments": ["answered_by"],
      "scope_policy": "Requires an explicit phase-scoped architect grant with frozen repo/base_branch scope, verifies the target WorkRequest is in scope, and verifies question_id belongs to that WorkRequest before mutating. Sibling question ids fail closed as not_found without leaking sibling content. This tool does not apply dashboard-only lifecycle helper policy.",
      "response_policy": "Returns safe JSON with the redacted updated clarification question, a minimal parent WorkRequest status projection, scope, and status metadata. It excludes read_work_request detail fields, work-key secrets, API tokens, private handoff payloads, and worker secret material."
    },
    {
      "name": "close_work_request_question",
      "purpose": "Close an open clarification question that belongs to a scoped WorkRequest without recording an answer.",
      "required_capabilities": ["write:work_request"],
      "required_arguments": ["work_request_id", "question_id", "current_status"],
      "scope_policy": "Requires an explicit phase-scoped architect grant with frozen repo/base_branch scope, verifies the target WorkRequest is in scope, and verifies question_id belongs to that WorkRequest before mutating. Sibling question ids fail closed as not_found without leaking sibling content. This tool does not apply dashboard-only lifecycle helper policy.",
      "response_policy": "Returns safe JSON with the redacted updated clarification question, a minimal parent WorkRequest status projection, scope, and status metadata. It excludes read_work_request detail fields, work-key secrets, API tokens, private handoff payloads, and worker secret material."
    },
    {
      "name": "record_work_request_decision",
      "purpose": "Record a durable decision log entry on a scoped WorkRequest.",
      "required_capabilities": ["write:work_request"],
      "required_arguments": ["work_request_id", "source_type", "decision", "rationale", "scope_impact", "created_by"],
      "optional_arguments": ["source_id"],
      "argument_constraints": {
        "source_type": ["human", "architect", "operator", "ask_pro_advisory"]
      },
      "scope_policy": "Requires an explicit phase-scoped architect grant with frozen repo/base_branch scope and verifies the target WorkRequest is in scope before recording the decision. This tool does not apply dashboard-only lifecycle helper policy.",
      "response_policy": "Returns safe JSON with the redacted created decision log entry, a minimal parent WorkRequest status projection, scope, and status metadata. It excludes read_work_request detail fields, work-key secrets, API tokens, private handoff payloads, and worker secret material."
    },
    {
      "name": "add_work_request_planned_slice",
      "purpose": "Add a planned slice to a scoped WorkRequest.",
      "required_capabilities": ["write:work_request"],
      "required_arguments": ["work_request_id", "title", "goal", "work_package_kind", "target_base_branch", "owned_file_globs", "forbidden_file_globs", "acceptance_criteria", "validation_steps", "review_lanes", "stop_conditions"],
      "optional_arguments": ["branch_pattern"],
      "argument_constraints": {
        "work_package_kind": ["quick_fix", "hotfix", "investigation", "adapter", "mcp", "skill", "hooks"]
      },
      "scope_policy": "Requires an explicit phase-scoped architect grant with frozen repo/base_branch scope, verifies the target WorkRequest is in scope, and validates owned_file_globs against the parent WorkRequest path constraints before creating the planned slice. `**` must be a complete path segment; valid examples include scripts/**/deploy*.ps1 and .github/workflows/**, while scripts/**deploy** and packages/**kraken_batch** are rejected with structured safe validation details. It does not dispatch planned slices, create WorkPackages, alter SecretHandoff, mutate Linear, run automatic slicing/package generation, or change dashboard behavior.",
      "response_policy": "Returns safe JSON with the redacted created planned slice, a minimal parent WorkRequest status projection, scope, and status metadata. It excludes read_work_request detail fields, work-key secrets, API tokens, private handoff payloads, and worker secret material."
    },
    {
      "name": "approve_work_request_planned_slice",
      "purpose": "Approve a planned slice that belongs to a scoped WorkRequest.",
      "required_capabilities": ["write:work_request"],
      "required_arguments": ["work_request_id", "planned_slice_id", "current_status"],
      "scope_policy": "Requires an explicit phase-scoped architect grant with frozen repo/base_branch scope, verifies the target WorkRequest is in scope, verifies planned_slice_id belongs to that WorkRequest, and validates owned_file_globs against the parent WorkRequest path constraints before approving. Sibling slice ids fail closed as not_found without leaking sibling content. Invalid `**` placement inside a segment returns structured safe validation details with field, value, and reason such as unsupported_globstar.",
      "response_policy": "Returns safe JSON with the redacted updated planned slice, a minimal parent WorkRequest status projection, scope, and status metadata. It excludes read_work_request detail fields, work-key secrets, API tokens, private handoff payloads, and worker secret material."
    },
    {
      "name": "skip_work_request_planned_slice",
      "purpose": "Skip a planned slice that belongs to a scoped WorkRequest.",
      "required_capabilities": ["write:work_request"],
      "required_arguments": ["work_request_id", "planned_slice_id", "current_status"],
      "scope_policy": "Requires an explicit phase-scoped architect grant with frozen repo/base_branch scope, verifies the target WorkRequest is in scope, and verifies planned_slice_id belongs to that WorkRequest before mutating. Sibling slice ids fail closed as not_found without leaking sibling content. The existing service preserves the last active slice requirement for already-sliced WorkRequests.",
      "response_policy": "Returns safe JSON with the redacted updated planned slice, a minimal parent WorkRequest status projection, scope, and status metadata. It excludes read_work_request detail fields, work-key secrets, API tokens, private handoff payloads, and worker secret material."
    },
    {
      "name": "mark_work_request_sliced",
      "purpose": "Mark a scoped WorkRequest sliced using the existing approved-slice requirement.",
      "required_capabilities": ["write:work_request"],
      "required_arguments": ["work_request_id", "current_status"],
      "scope_policy": "Requires an explicit phase-scoped architect grant with frozen repo/base_branch scope and verifies the target WorkRequest is in scope before calling WorkRequestService.mark_sliced. Legacy null phase_id architect grants, missing snapshots, drifted anchors, worker sessions, anonymous sessions, stale sessions, revoked sessions, and architect sessions without write:work_request fail closed.",
      "response_policy": "Returns safe JSON with a minimal updated WorkRequest status projection plus scope and status metadata. It preserves the existing approved-or-dispatched planned-slice requirement and excludes read_work_request detail fields, work-key secrets, API tokens, private handoff payloads, and worker secret material."
    },
    {
      "name": "dispatch_work_request_planned_slice",
      "purpose": "Dispatch one approved planned slice into a WorkPackage and private worker handoff.",
      "required_capabilities": ["dispatch:work_request"],
      "required_arguments": ["work_request_id", "planned_slice_id", "claimed_by"],
      "optional_arguments": ["secret_handoff", "secret_store_dir", "symphony_repo_root"],
      "scope_policy": "Requires an explicit phase-scoped architect grant with frozen repo/base_branch scope and verifies the target WorkRequest and planned_slice_id are in scope before dispatch. It uses the existing PlannedSliceDispatch.dispatch orchestration, has a statically discoverable schema, accepts symphony_repo_root as the optional Symphony++ helper/namespace repo root, keeps repo_root only as a hidden legacy alias, defaults to configured or discoverable local Symphony++ root when possible, and requires that root to contain the worker secret helper script under scripts/. It requires a file-backed live database ledger for worker handoff, treats blank database configuration as absent, preserves matching configured SQLite file URI options including the default local ledger, and fails closed for missing or invalid helper root, in-memory database, divergent explicit MCP database, read-only SQLite URI options, out-of-scope, missing, non-approved, invalid-status, unsupported-kind, and slice-scope-violation cases. Scope/glob failures return structured safe validation details with field, value, and reason.",
      "response_policy": "Returns safe JSON with WorkRequest id, planned slice id/status/work_package_id/dispatched_at, WorkPackage id metadata, and redacted worker handoff metadata. Dispatch-link failure responses may include sanitized WorkPackage id, worker grant id, cleanup status, and redacted handoff metadata. It never returns raw worker secrets, work keys, bearer tokens, API tokens, MCP auth tokens, private-store secret payloads, or secret-bearing claim URLs."
    },
    {
      "name": "prepare_work_package_worktree",
      "purpose": "Prepare a scoped WorkPackage git worktree under CODEX_HOME/worktrees/spp_worktrees and record only worktree_path.",
      "required_capabilities": ["dispatch:work_request"],
      "required_arguments": ["work_package_id", "target_repo_root", "base_branch", "branch"],
      "optional_arguments": ["worktree_parent"],
      "scope_policy": "Requires an explicit phase-scoped architect grant with frozen repo/base_branch scope and verifies the WorkPackage is linked from a planned slice on an in-scope WorkRequest. It treats target_repo_root as the target product repository used for git ref validation, fetch, and worktree operations, validates target_repo_root against the scoped WorkPackage repository, requires base_branch to match the WorkPackage, resolves CODEX_HOME with fallback to ~/.codex, constructs a safe path under CODEX_HOME/worktrees/spp_worktrees/<repo-name>-<repo-hash>/<package-id>-<sanitized-branch>-<branch-hash>, fetches origin/<base_branch> into the local remote-tracking ref, creates the worktree from origin/<base_branch>, records only worktree_path, and replays already_prepared when the same recorded path still exists.",
      "response_policy": "Returns safe JSON with WorkPackage id/kind/status/worktree_path, worktree path/status/target_repo_root/branch/base_branch, a compact worker launch block, scope, and redacted audit event metadata. Git failures return sanitized status, stderr, target repo root, worktree destination, branch, and base branch diagnostics. It never returns raw worker secrets, work keys, bearer tokens, API tokens, MCP auth tokens, private-store secret payloads, or secret-bearing claim URLs."
    },
    {
      "name": "cleanup_work_package_worktree",
      "purpose": "Clean up a scoped WorkPackage git worktree after validating the recorded path and dirty state.",
      "required_capabilities": ["dispatch:work_request"],
      "required_arguments": ["work_package_id", "target_repo_root"],
      "optional_arguments": [],
      "scope_policy": "Requires an explicit phase-scoped architect grant with frozen repo/base_branch scope and verifies the WorkPackage is linked from a planned slice on an in-scope WorkRequest. It requires target_repo_root, reads the recorded worktree_path, verifies it remains under CODEX_HOME/worktrees/spp_worktrees, refuses dirty worktrees by default, proves the recorded worktree belongs to the supplied target_repo_root when cleanup acts on a recorded worktree, removes the git worktree, prunes git worktree metadata, clears worktree_path, and records redacted audit/progress evidence. Missing recorded paths replay as already_clean.",
      "response_policy": "Returns safe JSON with WorkPackage id/kind/status/worktree_path, cleanup status/path metadata, scope, and redacted audit event metadata when cleanup acted on a recorded path. It never force-removes dirty worktrees and never returns raw worker secrets, work keys, bearer tokens, API tokens, MCP auth tokens, private-store secret payloads, or secret-bearing claim URLs."
    },
    {
      "name": "read_child_status",
      "purpose": "Read the architect grant's scoped anchor package status or a same-phase child work-package status.",
      "required_capabilities": ["read:child_progress", "read:child_findings"],
      "scope_policy": "Anchor reads remain package-scoped; child reads require the child package to be in the architect grant's current phase and match the phase anchor repo/base branch. Missing arbitrary child IDs fail closed with the same scoped-denial shape as out-of-scope children."
    },
    {
      "name": "read_phase_board",
      "purpose": "Read the architect grant's scoped phase board filtered to the frozen repo/base branch for explicit phase grants, including merged-child phase progress.",
      "scope_policy": "Explicit phase architect grants require the frozen repo/base-branch snapshot; matching grants materialize only same-snapshot package cards, and missing snapshots fail closed without unsafe backfill. Phase progress summary totals are computed after backend authorization and frozen repo/base scope filters, then remain stable across client-side board filters. Abandoned children are excluded. Closed children still count in the merge-progress denominator but are not open work."
    },
    {
      "name": "request_child_replan",
      "purpose": "Phase 7 stub for child replan requests; returns phase7_not_implemented after architect authorization."
    },
    {
      "name": "approve_child_ready_state",
      "purpose": "Approve a same-phase phase_child in ready_for_architect_merge after readiness gates still pass; records a local audit event and moves the child to merging_into_phase. Optional request_id is the explicit retry identity.",
      "scope_policy": "Revalidates the explicit phase grant, frozen repo/base-branch anchor scope, phase-child parent/file scope, and active phase state before approval. No override is part of this contract. Exact retries replay across renewed architect grants for the same actor only when the caller supplies the same request_id; rationale is audit text and is not idempotency key material. Same-request_id retries within the same ready-state cycle replay the original approval audit event; retry rationale edits do not mutate the recorded audit rationale. Re-approval after a real blocked/reworked/readied cycle records a new approval audit event even when the same request_id and rationale text are reused. After approval, the child worker may only move merging_into_phase back to blocked to represent a merge blocker before further worker follow-up."
    },
    {
      "name": "merge_child_into_phase",
      "purpose": "Record a local merge artifact for an approved phase child and move it to merged_into_phase; does not perform a live Git merge.",
      "artifact_policy": "merge_artifact.status must be merged_into_phase and merge_artifact.uri must be nonempty. The tool records a phase_merge artifact on the child package and preserves the full accepted merge_artifact as artifact metadata. Exact retries replay across renewed scoped architect grants even when the renewed grant has a different claimed_by; after the child is already merged, a new valid merge artifact updates the local phase_merge record and appends an audit event without another lifecycle transition while the phase remains active. Exact older retries do not overwrite newer artifact data. Merge finalization and post-merge artifact updates are rejected after the phase closes."
    },
    {
      "name": "split_work_package",
      "purpose": "Phase 7 stub for child package splitting; returns phase7_not_implemented after architect authorization."
    },
    {
      "name": "publish_phase_update",
      "purpose": "Phase 7 stub for phase updates; returns phase7_not_implemented after architect authorization."
    }
  ],
  "architect_authorization_policy": "Architect tools require a live architect grant session and the matching architect capability. WorkRequest read tools require read:work_request plus an explicit phase-scoped architect grant, WorkRequest mutation tools require write:work_request plus that same explicit phase-scoped grant, and planned-slice dispatch requires dispatch:work_request plus that same explicit phase-scoped grant. These tools use only the grant's frozen repo/base-branch scope after revalidating the claimed phase anchor. Legacy null phase_id architect grants are not supported for WorkRequest MCP reads, mutations, or dispatch and fail closed instead of deriving scope from a mutable anchor package. Phase architect grants are revalidated against the frozen phase/repo/base-branch anchor scope before phase-dependent actions; scoped explicit phase-board reads and P7 child delegation/status operations fail closed if that snapshot is missing. Missing snapshots are not backfilled from mutable anchor state. Worker grants and architect grants without the required capability are denied. Worker grants cannot be minted with architect-only MCP capabilities.",
  "architect_discovery_policy": "tools/list uses static architect schema discovery. Healthy unbound generic sessions advertise health, Solo Session tools, claim_work_key, claim_private_handoff, create_work_request, and architect tool schemas so fresh Codex sessions can discover WorkRequest and architect flows before claim. Bound architect sessions advertise health, get_current_assignment, and the same static architect schema set. Schema visibility is not authorization: architect calls still require a live claimed architect grant with the required capability, frozen phase/repo/base-branch scope when applicable, and all handler-specific checks. Unclaimed architect calls return claim_required, insufficient grants return permission denials, and stale or revoked bound sessions expose only health, claim_work_key, and claim_private_handoff for refresh. Duplicate initialize on that same stale explicit MCP session does not downgrade it into generic unbound discovery; recovery is re-claim on that session or a new MCP process/session. Worker sessions receive the bound worker-facing tool surface without Solo tools or architect-only schemas. Lifecycle capabilities such as architect:lifecycle.transition do not authorize MCP architect tool calls; explicit MCP capability strings are required at call time.",
  "stdio_transport_policy": "Stdio accepts newline-delimited JSON-RPC messages. Large responses from tools/list or read_work_request can fill stdout pipes, so one-shot callers must drain stdout concurrently or redirect stdout to a file instead of waiting for process exit before reading output. The private-file wrapper provides run-mcp-local-file-once for diagnostics that need to send a JSONL request file and spool stdout/stderr to files without printing the work key; caller-supplied output/error files must not already exist, and generated spool files are used when output/error files are omitted. JSON-RPC batch arrays are not ordered session transactions: claim_work_key and claim_private_handoff do not authorize later non-claim items in the same batch, a successful claim applies to later standalone requests, and later claim entries in that same batch are rejected as rebinding attempts.",
  "resources": [
    "sympp://assignment/current",
    "sympp://work-packages/{id}/context.md",
    "sympp://work-packages/{id}/task_plan.md",
    "sympp://work-packages/{id}/findings.md",
    "sympp://work-packages/{id}/progress.md",
    "sympp://work-packages/{id}/acceptance.md",
    "sympp://work-packages/{id}/review_suite.md",
    "sympp://work-packages/{id}/handoff.md"
  ]
}
