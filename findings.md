# Findings & Decisions: SYMPP-P3-002

## Requirements

- Implement worker MCP tools/resources from `implementation_docs_symphplusplus/work_packages/SYMPP-P3-002_worker-mcp-tools-and-resources.md`.
- Acceptance requires a worker lifecycle for its own package, sibling package denial, readiness gate enforcement, and actor/grant-scoped idempotent writes where appropriate.
- Hard scope excludes architect tools, skill package, dashboard/API, broader GitHub sync, Linear live state, and unrelated cleanup.

## Initial Discovery

- P3-001 scaffold already provides JSON-RPC/MCP initialization, STDIO, health tool, version/current-assignment resources, session proof revalidation, and protected-resource denial stubs.
- Current P3-001 behavior intentionally returns `worker_resources_not_implemented` for authorized work-package resource reads; P3-002 owns replacing that stub with virtual planning resources.
- MCP contract lists worker tools: `claim_work_key`, `get_current_assignment`, `read_context`, `read_task_plan`, `update_task_plan`, `append_finding`, `append_progress`, `set_status`, `report_blocker`, `request_scope_expansion`, `attach_branch`, `attach_pr`, `submit_review_package`, and `mark_ready`.
- Required resources are `sympp://assignment/current` plus scoped work-package Markdown resources for context, task plan, findings, progress, acceptance, review suite, and handoff.
- Planning renderers already own all required virtual Markdown file rendering, so P3-002 can expose them without duplicating Markdown formatting.
- Existing audited progress events already provide grant-scoped idempotency for progress-like writes; branch, PR, review package, blocker, and scope-expansion metadata are recorded as typed scoped progress events.
- The core lifecycle service only supports selected policy kinds from P1-003. To avoid broad lifecycle changes, P3-002 keeps `set_status` on the lifecycle service and implements `mark_ready` as the MCP readiness tool with local gates and terminal ready status selection.

## Open Questions

- Full P6 readiness concerns such as changed-file scope, CI sync, stale review artifacts, and GitHub merge state remain out of scope for P3-002.

## Decisions

- Keep tool registration and dispatch inside the existing MCP server unless discovery shows a local P3-001 extension point intended for worker tools.
- Use existing access-grant/session proof checks for every scoped read/write rather than trusting request-supplied work package ids.
- Require explicit `idempotency_key` for general `append_progress`; derive deterministic keys for branch/PR/review metadata tools.
- Reject request-supplied sibling `work_package_id` values on worker writes instead of silently applying them to the current package.
- T1 found `mark_ready` should use the lifecycle service rather than direct status updates so worker capability checks still apply.
- T1 found readiness evidence must come from protected metadata tools rather than free-form `append_progress` payloads.
- T1 found blocker and scope-expansion tool-owned payload fields must override caller-supplied payload metadata.
- T1 found non-map progress payloads should return a structured tool error rather than crashing during payload merge.
- T1 found skipped plan nodes should not block readiness; only pending plan nodes remain incomplete.
- Lifecycle support was widened only to the worker package kinds needed by P3 tooling (`mcp`, `skill`, `hooks`) plus existing supported kinds, leaving broader kinds such as `docs` and `standard_pr` unchanged to preserve current tracker behavior.
- Second T1 found batch requests must thread the updated MCP server state so `claim_work_key` can bind a session for later batch items.
- Second T1 found `tools/list` should advertise real per-tool worker argument schemas instead of the generic unconstrained object schema.
- T2 found `update_task_plan` must support the documented patch plus `expected_version` contract so existing pending plan nodes can be completed instead of duplicated.
- T2 found `report_blocker` needs a matching worker path to clear transient blockers before readiness; the MCP surface now includes `resolve_blocker`.
- T2 found `submit_review_package` must reject empty review evidence before satisfying readiness.
- T2 found `append_finding` needs idempotency across client retries; the worker MCP path now derives a stable finding id from the grant/package/idempotency key.
- T2 found new lifecycle-supported worker package kinds need policy templates so review-suite resources do not render unknown-policy output.
- Second T2 found every `update_task_plan` write path must require `expected_version`; append-style node creation now also goes through version checking.
- Second T2 found multi-node plan patches must be atomic; patch application now runs inside a repo transaction and rolls back on any failed node.
- Second T2 found `mark_ready` must enforce required review lanes from policy templates, not only the presence of arbitrary review metadata.
- Third T2 found `set_status` must not bypass readiness gates by transitioning directly to ready statuses; ready transitions now require `mark_ready`.
- Third T2 found review-lane readiness evidence must be structured review entries, not arbitrary free text in summary/tests/artifacts.
- Third T2 found malformed plan patch nodes must return tool errors instead of crashing during transaction rollback.
- Third T2 found storage/transaction failure classes should surface as MCP service errors rather than invalid user params.

## Takeover Findings

- The handoff's dirty `server.ex` change is no longer dirty in this worktree; it was committed and pushed as `55748aa` (`fix(mcp): tighten worker readiness gates`).
- `review-state` on `55748aa` recommends a fresh full-diff `review_t2` because the current branch has exceeded the narrow follow-up churn threshold.
- PR #15 currently has no GitHub comments, reviews, or status checks reported by `gh pr view`.
- Fresh T2 round `phase_gate-symphony-plus-plus-sympp-p3-002-e4d006-20260502T190153Z-5b4edd37` produced valid findings: `submit_review_package` schema omitted required `reviews` evidence, malformed patch ids could append duplicate nodes, and review readiness was not tied to the latest attached PR head.
- Fourth T2 fix keeps scope inside P3-002: advertise `reviews`/`head_sha`, reject non-string patch ids with `invalid_patch_node`, and accept review-lane evidence only for the latest attached PR `head_sha` when one is known.
- Fresh T2 round `phase_gate-symphony-plus-plus-sympp-p3-002-e4d006-20260502T190914Z-8b11cd9e` produced valid findings: `attach_pr` still allowed missing `head_sha`, `update_task_plan` schema did not require `expected_version`, and malformed review lane/verdict values could crash readiness checks.
- Fifth T2 fix requires PR `head_sha` at schema and runtime, advertises `expected_version` as required, and ignores review entries whose lane/verdict fields are not strings.
- Fresh T2 round `phase_gate-symphony-plus-plus-sympp-p3-002-e4d006-20260502T191607Z-dbf3be27` produced valid findings: non-map `patch` payloads could append nodes, `claim_work_key` performed a second post-claim grant read, investigation readiness skipped recommendation evidence, and metadata tools honored caller idempotency keys.
- Sixth T2 fix rejects non-map patches, builds claim sessions from the returned assignment, requires investigation recommendation evidence via `request_scope_expansion`, and forces metadata tools to use deterministic tool-owned idempotency keys.
- Fresh T2 round `phase_gate-symphony-plus-plus-sympp-p3-002-e4d006-20260502T192507Z-23d427e8` produced valid findings: claim replay after a lost response needed same-secret recovery, finding idempotency needed package scope and conflict detection, no-op plan patches needed rejection, and investigation recommendation evidence should not be modeled as scope expansion.
- Seventh T2 fix recovers claimed sessions from the same work-key secret, derives finding ids from package plus idempotency key, rejects conflicting finding retries, rejects no-op plan-node patches, and accepts a dedicated `append_progress` recommendation payload for investigation readiness.
- Fresh T2 round `phase_gate-symphony-plus-plus-sympp-p3-002-e4d006-20260502T193553Z-2c55c78d` produced valid findings: readiness should require review artifacts, metadata tool retries should survive worker reclaims, and progress-event idempotency keys should be tool-namespaced.
- Eighth T2 fix requires non-empty current-head review artifacts before readiness, grant-scopes metadata tool idempotency, and namespaces progress idempotency by tool.
- Fresh T2 round `phase_gate-symphony-plus-plus-sympp-p3-002-e4d006-20260502T194714Z-4e98e8d5` produced valid findings: `submit_review_package` accepted headless evidence after PR attachment, malformed array fields were silently dropped, review artifacts were not persisted into canonical artifact resources, and `read_task_plan` used separate state reads for text and version.
- Ninth T2 fix validates required review arrays at submission time, requires `head_sha` once a PR head exists, persists submitted review artifacts, and renders task-plan text/version from the same state snapshot.
- Fresh T2 round `phase_gate-symphony-plus-plus-sympp-p3-002-e4d006-20260502T195650Z-c5feb960` produced valid findings: review package submission needed atomic metadata/artifact persistence, blank artifact entries could satisfy readiness, schema required fields were stale, readiness needed persisted artifact rows, and stale PR-head review packages should fail fast.
- Tenth T2 fix wraps review metadata plus artifacts in one transaction, rejects blank artifact strings, advertises artifacts/reviews as required, validates submitted review `head_sha` against the latest attached PR head, and bases artifact readiness on persisted review artifacts.
- Fresh T2 round `phase_gate-symphony-plus-plus-sympp-p3-002-e4d006-20260502T200742Z-797c616f` produced valid findings: claimed work keys should not be replayed into a new server session, review artifact dedupe/readiness needed PR-head specificity, and `submit_review_package` needed the same scoped `work_package_id` guard as other write tools.
- Eleventh T2 fix rejects already-claimed work-key replays, keys persisted review artifacts by work package plus PR head plus artifact path, and routes review package submission through `scoped_session/3`.
- Fresh T2 round `phase_gate-symphony-plus-plus-sympp-p3-002-e4d006-20260502T201546Z-42372871` produced valid findings: readiness needed the latest review verdict per lane, artifact readiness needed every declared current-head artifact, quick-fix policy should not be forced through merge metadata, timestamp-only plan versions could miss concurrent writes, and malformed review entries should fail at submission.
- Twelfth T2 fix makes latest review verdicts authoritative, requires all declared persisted current-head artifacts when merge policy requires artifacts, derives merge metadata gates from policy required gates, uses a content-derived plan version token, and rejects malformed review entries in `submit_review_package`.
- Fresh T2 round `phase_gate-symphony-plus-plus-sympp-p3-002-e4d006-20260502T202615Z-8f9e500a` produced valid findings: progress/metadata write tools needed successful idempotency replay on lost-response retries, and `claim_work_key` needed to classify ledger/storage failures as server errors instead of auth failures.
- Thirteenth T2 fix replays existing progress events on idempotency-key conflict and maps claim ledger failures through the existing service-error path.
- Fresh T2 round `phase_gate-symphony-plus-plus-sympp-p3-002-e4d006-20260502T203648Z-ab64dde9` produced valid findings: `update_task_plan` needed to return the plan version from the same transaction as the write, `submit_review_package` needed to dedupe duplicate artifact paths inside a single submission, and progress idempotency replay needed to reject retries whose content changed.
- Fourteenth T2 fix returns updated plan nodes plus version from the transaction, normalizes submitted artifacts with `Enum.uniq/1`, and replays progress writes only when summary/body/status/payload match the stored event.
- Fresh T2 round `phase_gate-symphony-plus-plus-sympp-p3-002-e4d006-20260502T204729Z-944e7454` produced valid findings: response-only `Server.handle/2` callers could drop initialized/session state after `claim_work_key`, notification-based `claim_work_key` calls did not bind the session, and an already-bound server could pivot to another work key.
- Fifteenth T2 fix adds per-server response-only state retention without changing the `handle/2` response shape, binds claim notifications inside `handle_state/2`, allows same-secret bound-session replay, and rejects different-secret rebind attempts.
- Fresh T2 round `phase_gate-symphony-plus-plus-sympp-p3-002-e4d006-20260502T205743Z-809031fd` produced valid findings: same-secret bound claim replay needed live grant revalidation, worker MCP claim needed to reject non-worker grants, response-only state retention should not leak entries for unchanged one-shot calls, `report_blocker` schema omitted `blocker_id`, and review lane/verdict normalization needed trimming.
- Sixteenth T2 fix revalidates bound claim replays through the access-grant repository, requires worker grant role at claim time, persists response-only state only when session changes, advertises `blocker_id` for `report_blocker`, and trims normalized review lane/verdict strings.
- Fresh T2 round `phase_gate-symphony-plus-plus-sympp-p3-002-e4d006-20260502T210904Z-daebabe8` produced valid findings: response-only `Server.handle/2` needed to retain initialized handshake state, non-worker work keys needed rejection before mutating grant claim state, and `report_blocker` needed to reject non-string blocker ids before storing active blocker state.
- Seventeenth T2 fix restores initialized-state retention for response-only flows, preflights work-key role from the secret hash before `AccessGrantService.claim/3`, and validates optional `report_blocker.blocker_id` as a string before appending blocker progress.
- High-pressure coherence review after `review_state` showed the full diff is still centered on the P3-002 worker MCP server/test implementation plus small required support, while the current findings are narrow edge-case contract hardening rather than architectural churn.
- Fresh T2 round `phase_gate-symphony-plus-plus-sympp-p3-002-e4d006-20260502T211835Z-bd817a2a` produced valid findings: generated `append_finding` ids needed grant-scoped idempotency and the tool needed to preserve caller-supplied finding ids advertised in the schema.
- Eighteenth T2 fix preserves non-blank caller-provided finding ids and only generates grant-scoped finding ids when `id` is omitted.
- Fresh T2 round `phase_gate-symphony-plus-plus-sympp-p3-002-e4d006-20260502T212729Z-765eb917` produced valid findings: progress-style tools could report a successful normalized write as an idempotency conflict, explicit-id finding retries needed durable idempotency across changed caller ids, and review artifact readiness needed to follow the latest current-head review package.
- Nineteenth T2 fix pre-reads progress idempotency keys so new normalized writes return success while duplicate replays compare against normalized payloads, adds durable grant-scoped finding idempotency metadata, and evaluates review artifacts from the latest current-head review package event.
- Fresh T2 round `phase_gate-symphony-plus-plus-sympp-p3-002-e4d006-20260502T213807Z-71f13191` produced valid findings: the finding idempotency schema change needed a follow-up migration for existing ledgers, and duplicate explicit finding ids needed to reject unrelated idempotency keys instead of replaying by content only.
- Twentieth T2 fix restores the original planning-state migration, adds a forward migration for finding idempotency fields/index, and requires duplicate finding-id replays to match the same grant-scoped idempotency metadata before returning success.
- Fresh T2 round `phase_gate-symphony-plus-plus-sympp-p3-002-e4d006-20260502T214700Z-76b91f23` produced valid findings: `mark_ready` did not enforce policy-required acceptance/test gates, review-lane readiness merged stale older packages, `submit_review_package` broke the documented `summary/tests/artifacts` shape, injected non-worker sessions bypassed worker-only mutation checks, and finding idempotency keys were not whitespace-normalized.
- Twenty-first T2 fix keeps the existing P3-002 design and hardens the reported seams: review packages now keep `reviews` and `head_sha` optional while defaulting the current PR head, readiness checks latest-package acceptance/tests/review lanes, worker mutations reject non-worker sessions, and finding idempotency keys are trimmed before ID generation and persistence.
- Fresh T2 round `phase_gate-symphony-plus-plus-sympp-p3-002-e4d006-20260502T220447Z-59704ea0` produced valid findings: pre-bound non-worker sessions could read worker resources, `set_status` lacked the documented expected-state guard, and blocker ids were persisted without trimming so report/resolve retries could diverge.
- Twenty-second T2 fix applies the worker-role guard to virtual resource reads/listing and read tools, requires `expected_status` before `set_status` transitions, and normalizes blocker ids both when writing and when replaying readiness state.
- Fresh T2 round `phase_gate-symphony-plus-plus-sympp-p3-002-e4d006-20260502T221813Z-3d3e7d0d` produced valid findings: already-claimed worker keys could not reconnect after MCP restart, `get_current_assignment` skipped the worker-role guard, explicit empty `reviews` arrays were rejected despite being equivalent to omission, and invalid `acceptance_criteria_met` values were silently recorded as false.
- Twenty-third T2 fix rebuilds sessions from already-claimed worker grants using the same secret proof, applies the worker-role guard to `get_current_assignment`, accepts explicit empty review arrays, and rejects non-boolean acceptance evidence with `invalid_acceptance_criteria_met`.
- Fresh T2 round `phase_gate-symphony-plus-plus-sympp-p3-002-e4d006-20260502T222755Z-c421b7a0` produced valid findings: `submit_review_package` accepted unusable `tests` entries, `set_status.reason` was silently dropped, progress idempotency keys were not whitespace-normalized before tool prefixing, and empty task plans could satisfy `plan_complete`.
- Twenty-fourth T2 fix validates review-package tests as non-blank strings, records non-blank status reasons as status-transition progress events, trims progress idempotency keys before prefixing, and treats empty non-investigation plans as incomplete.
- Fresh T2 round `phase_gate-symphony-plus-plus-sympp-p3-002-e4d006-20260502T223850Z-5b69a018` produced valid findings: non-string `set_status.reason` could crash after validation fallthrough, reason event persistence could fail after a status transition, expected-status checks could race the final transition, and `mark_ready` readiness gates were checked from a pre-transition snapshot.
- Twenty-fifth T2 fix validates optional status reasons before side effects, wraps `set_status` in a worker transaction that appends the reason event and transitions from the same status snapshot, adds a lifecycle transition entry point for already-fetched snapshots with conditional status writes, and wraps `mark_ready` readiness gates plus ready transition in the same worker transaction.
- Fresh T2 round `phase_gate-symphony-plus-plus-sympp-p3-002-e4d006-20260502T225037Z-493948ab` produced valid findings: already-claimed work-key reconnect needed an owner check, explicit-id `append_finding` retries could silently return the earlier id, finding idempotency replay needed a visibility retry window after unique conflicts, quick-fix readiness should not require merge-only review-package metadata, and response-only `Server.handle/2` needed a stable default state key for recreated servers.
- Twenty-sixth T2 fix requires reconnecting claims to use the same `claimed_by`, compares finding ids during idempotent replay, retries finding replay lookups briefly after insert conflicts, limits the `review_package_submitted` readiness gate to merge-required policies, and uses a stable per-repo default state key for response-only `handle/2` callers.
